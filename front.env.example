# URL of the backend API — must be reachable from the user's browser
PUBLIC_API_URL=http://localhost:8080

# "local" (default) or "production"
# Controls environment-specific UI features:
#   local:      "Change server" button, /setup page for Capacitor native builds
#   production: SaaS-only features (billing, plan management, etc.)
PUBLIC_ENV=local
