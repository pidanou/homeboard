# URL of the backend API — must be reachable from the user's browser
VITE_API_URL=http://localhost:8080

# "local" (default) or "production"
# Controls environment-specific UI features:
#   local:      single-household mode
#   production: SaaS-only features ("New household" button, billing, etc.)
VITE_APP_ENV=local
