# URL of the backend API — must be reachable from the user's browser
API_URL=http://localhost:8080

# Public URL this frontend is served at — required by SvelteKit to validate
# form submissions (set to your real domain in production)
ORIGIN=http://localhost:3000
