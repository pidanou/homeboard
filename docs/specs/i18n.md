# Multi-language Support — Functional Spec

## Core concept

Family Board supports **English, French, Spanish, and German**. Translation happens at compile time via `@inlang/paraglide-js` — every user-facing string is a message key resolved to the active locale's text, tree-shaken per route, with no runtime translation fetch. There is no URL-based locale routing (`/fr/board`); the app is a fully client-rendered SPA (`ssr = false`), so the active locale is resolved and applied programmatically instead.

---

## Locale resolution order

Resolved once on app boot (`(app)/+layout.svelte`'s `onMount`), highest priority last:

1. **Browser default** — if no locale has ever been set client-side, `navigator.language` is checked against the supported set (`en`, `fr`, `es`, `de`); unsupported values fall back to `en`.
2. **`localStorage`** — paraglide's own persistence strategy. Once a locale has been set client-side (by step 1, or by an explicit change), it's read back on every subsequent load, before the profile is fetched — this keeps first paint in the right language without waiting on a network round-trip.
3. **Profile `locale`** — once `loadCurrentUser()` resolves, the account's stored `locale` always wins. If it differs from the currently active locale, `setLocale()` is called to override whatever browser/localStorage guess was showing. This is what makes the choice sync across devices.

Changing the language in **Profile → Language** calls `PATCH /api/v1/profile` with the new `locale`, then `setLocale()` to apply it (which triggers `paraglide-js`'s default `reload: true` behavior — a full page reload re-renders everything, including the layout's own locale-resolution effect, in the new language).

---

## Message keys

- Defined in `web/messages/{en,fr,es,de}.json` — flat, underscore-separated keys valid as JS identifiers (no nested objects, no dots).
- Called from Svelte files via `m.*()` or `msg.*()` (`import * as msg from '$lib/paraglide/messages.js'` — both aliases exist in the codebase; either is fine, pick whichever the file already uses).
- Parameterized messages use named placeholders, e.g. `"taskcard_due_at": "Due {date} at {time}"` → `msg.taskcard_due_at({ date, time })`.
- **Reuse over duplication:** if an English string is character-for-character identical to an already-defined key's value, reuse that key rather than minting a new one (e.g. `msg.dialog_cancel()`, `msg.action_add()` are reused across dozens of dialogs).
- Simple pluralization (member/members, profile/profiles) uses singular/plural key pairs selected by existing JS ternary logic — no ICU plural syntax, since the app's pluralization needs are small and don't warrant the added complexity.

---

## Date and time formatting

All `Intl`/`toLocaleDateString`/`toLocaleTimeString`/`toLocaleString` calls go through `web/src/lib/dates.ts`, which passes paraglide's `getLocale()` instead of `undefined` — so date/time formatting always follows the app's explicit language choice, not the browser's ambient locale (which may differ from what the user picked in-app).

---

## Backend

- `users.locale TEXT NOT NULL DEFAULT 'en'` (migration `000029_user_locale`).
- `AuthService.SetLocale` validates `locale` is one of `en`/`fr`/`es`/`de` (`supportedLocales` in `internal/service/auth.go`), rejecting anything else.
- `GET /api/v1/config` exposes `supportedLocales: ["en", "fr", "es", "de"]`. Note: the frontend's Profile → Language picker currently hardcodes its own `LANGUAGE_LABELS`/`locales` list (from paraglide) rather than reading this endpoint — the two lists must be kept in sync by hand.

---

## Backend error-message i18n

### Problem (as it was)
`client.ts`'s `request()` passed the raw HTTP error body straight into `toast.error()`. That body was plain English text from Go's `http.Error(w, "task not found", 404)` — 181 literal-string call sites plus 32 more forwarding a raw `err.Error()` and 1 string-concatenation call site (214 total) across `internal/handler/*.go`. A French or Spanish user got the entire UI translated except server-side error toasts, which were always English regardless of their locale.

### Design (as implemented)

**Backend**
- `internal/handler/errors.go`: `writeError(w http.ResponseWriter, status int, code string)` writes `{"error": "<code>"}` as JSON instead of a plain-text body. `codeFor(err error, fallback string) string` resolves a service-layer sentinel error to its stable code via `errors.Is`, falling back to a safe generic code for unexpected/wrapped internal errors so raw Go error text never reaches the client.
- Every `http.Error(w, "<english text>", status)` call site converted to `writeError(w, status, "<snake_case_code>")`. Identical semantics reuse the same code across handlers (`invalid_request`, `unauthorized`, `forbidden`, `name_required`, `ids_required`, …), the same "reuse over duplication" convention already used for frontend message keys — 214 call sites collapsed to **104 unique codes**.
- Call sites that forwarded `err.Error()` from the service layer needed sentinel errors added so the handler can map them without leaking raw text: `ErrUnauthorized`/`ErrAdminRequired`/`ErrForbidden` (`internal/handler/middleware.go`), `ErrNoPasswordSet`/`ErrInvalidCurrentPassword` (`internal/service/auth.go`), the household admin/role sentinels (`internal/service/household.go`), and the invite-lifecycle sentinels (`internal/service/invite.go`). These are registered in a package-level `errorCodes map[error]string` in `internal/handler/errors.go`.
- No error-code enum/registry beyond that map — Go doesn't need to know what a code *means*, only the frontend does.

**Frontend**
- `web/src/lib/api/errors.ts`: a `Record<string, () => string>` (`errorMessages`) mapping each of the 104 backend codes to a `msg.error_*()` function, plus `resolveErrorMessage(res: Response)` — parses the body as JSON `{ error: code }`, resolves it through the lookup, and falls back to the raw response text (or `res.statusText`) when the body isn't JSON or the code isn't recognized. This is what makes handler-by-handler migration safe.
- `client.ts`'s `request()` and `upload()` both call `resolveErrorMessage(res)` in their `!res.ok` branch instead of reading `res.text()` directly, toast the resolved string, and `throw new Error(message)`.
- The thrown `Error`'s `.message` carries the resolved **localized** string, not the raw code — `profile/+page.svelte`'s `e.message` reads for inline field errors keep working unchanged.
- One `error_<code>` message key per unique backend code, added to all 4 locale files (`en`, `fr`, `es`, `de`).

### Rollout
Was safe to migrate handler-by-handler: an un-migrated handler still returns plain text, which fails the frontend's JSON parse and falls through to the raw-text toast — no breakage mid-migration. All 16 handler files are now converted; there are no remaining `http.Error` call sites in `internal/handler` (verified via grep).

### Out of scope
- Structured field-level validation (e.g. turning "title is required" into a per-field inline error) — this only localizes the message text, it doesn't change validation UX.
- Error codes as a versioned/stable public API contract — they're an internal detail between this Go backend and this SvelteKit frontend, not a documented contract for third-party API consumers.

---

## What's out of scope

- **ICU plural rules / gendered grammar.** Only simple singular/plural pairs are supported.
- **RTL languages.** Not needed for en/fr/es; would require layout-direction work if added later.
- **Per-family or per-content locale** (e.g. translating a task title). Locale is a per-user UI preference only.
- **Additional languages beyond en/fr/es/de.** Adding one is mechanical (new `messages/<locale>.json` + entry in `locales` + backend `supportedLocales` map) but not pre-built for.

---

## Delta vs current implementation

| Feature | Status |
|---|---|
| Paraglide scaffolding + build pipeline | Implemented |
| `locale` field on user profile (backend + sync) | Implemented |
| Locale resolution (browser detect → localStorage → profile) | Implemented |
| Full UI string sweep (chrome, auth, Today/Board, Lists, Calendar, Settings, Profile, shared dialogs) | Implemented |
| Date/time formatting via active locale | Implemented |
| Profile Language selector | Implemented |
| German (`de`) locale | Implemented |
| Backend error-message i18n | Implemented |
