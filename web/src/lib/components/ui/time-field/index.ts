import { TimeField as TimeFieldPrimitive } from 'bits-ui';
import Input from './time-field-input.svelte';

const Root = TimeFieldPrimitive.Root;

export {
	Root,
	Input,
	//
	Root as TimeField,
	Input as TimeFieldInput,
};
